version: '3.8'

services:
  privacy-service:
    build:
      context: ./services/privacy-service-py
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - ./services/privacy-service-py:/app
